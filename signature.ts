/**
 * Destructively modify the `project` object according to the domain-specific
 * language in the object's `options` property .
 */
export default async function tool(project: any): Promise<void> {
  /* Stuff happens here… */
}
